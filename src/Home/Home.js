import React, { useContext } from 'react';
import { Card, CardBody, CardTitle } from 'reactstrap';

function Home() {
	return (
		<div className="Home">
			<h1>Welcome! </h1>
		</div>
	);
}

export default Home;
